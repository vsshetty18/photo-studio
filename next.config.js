/**
 * next.config.js
 * -----------------
 * Minimal Next.js config. No special settings needed for this simple app —
 * kept here mainly because Next.js expects this file to exist.
 */

/** @type {import('next').NextConfig} */
const nextConfig = {};

module.exports = nextConfig;
